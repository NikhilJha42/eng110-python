# Tuples

instruments = ("piano", "guitar", "drums", "piano")

print(instruments.index("guitar"))
print(instruments.count("piano"))

# Tuples are immutable ordered collections. The above methods are the only two available for tuples.
print(instruments[0])